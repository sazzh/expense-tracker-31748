{
  "name": "expense-tracker-31748",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "frontend": "cd frontend && npm run dev",
    "backend": "cd backend && uv run uvicorn main:app --reload",
    "dev": "concurrently -n frontend,backend \"npm run frontend\" \"npm run backend\""
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}